#include <stdio.h>
/**
 * main - prints alphabet in upper and lower case
 * Return: zero if kawa
 */

int main(void)
{
	char chl;

	char chu;

	for (chl = 'a'; chl <= 'z'; chl++)
	{
	putchar(chl);
	}
	for (chu = 'A'; chu <= 'Z'; chu++)
	{
	putchar(chu);
	}
	return (0);
}

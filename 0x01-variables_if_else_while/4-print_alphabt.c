#include <stdio.h>
/**
 * main - prints entire alphabet but a few
 * Return: zero if thumbs up
 */

int main(void)
{
	char chl;

	for (chl = 'a'; chl <= 'z'; chl++)
	{
		if (chl == e || ch1 == q)
		{
			continue;
		}
	putchar(ch1);
	}
	putchar('\n');
	return (0);
}

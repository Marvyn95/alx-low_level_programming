#include "main.h"
/**
 * _abs - prints the absolute value of an integer
 * @t: is the input argument
 * Return: the absolute value
 */

int _abs(int t)
{
	if (t >= 0)
	{
		return (t);
	}
	else
	{
		return (-t);
	}
}

#include <stdio.h>

/**
 * main - prints its name
 * @argc: rgument count
 * @argv: argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}

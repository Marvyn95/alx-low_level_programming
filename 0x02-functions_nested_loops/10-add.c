#include "main.h"
/**
 * add - adds two integers and returns the result
 * Return: result of addition
 * @a: input parameter
 * @b: second input parameter
 */

int add(int a, int b)
{
	return (a + b);
}
